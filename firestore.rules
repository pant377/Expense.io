rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function signedInAs(userId) {
      return request.auth != null && request.auth.uid == userId;
    }

    function validProfile() {
      return request.resource.data.keys().hasOnly([
          'displayName',
          'email',
          'createdAt',
          'updatedAt'
        ])
        && request.resource.data.keys().hasAll([
          'displayName',
          'email',
          'createdAt',
          'updatedAt'
        ])
        && request.resource.data.displayName is string
        && request.resource.data.displayName.size() > 0
        && request.resource.data.displayName.size() <= 60
        && request.resource.data.email is string
        && request.resource.data.createdAt is timestamp
        && request.resource.data.updatedAt is timestamp
        ;
    }

    function validExpense(userId, expenseId) {
      return request.resource.data.keys().hasOnly([
          'description',
          'amountCents',
          'category',
          'transactionType',
          'paymentMethod',
          'currency',
          'photoStoragePath',
          'photoFileName',
          'photoContentType',
          'occurredAt',
          'createdAt',
          'updatedAt'
        ])
        && request.resource.data.keys().hasAll([
          'description',
          'amountCents',
          'category',
          'transactionType',
          'paymentMethod',
          'currency',
          'occurredAt',
          'createdAt',
          'updatedAt'
        ])
        && request.resource.data.description is string
        && request.resource.data.description.size() > 0
        && request.resource.data.description.size() <= 120
        && request.resource.data.amountCents is int
        && request.resource.data.amountCents > 0
        && request.resource.data.amountCents <= 100000000000
        && request.resource.data.category is string
        && request.resource.data.category.size() > 0
        && request.resource.data.category.size() <= 50
        && request.resource.data.transactionType in ['expense', 'income']
        && request.resource.data.currency in ['EUR', 'USD', 'GBP', 'CHF', 'JPY', 'CAD', 'AUD']
        && (
          request.resource.data.paymentMethod == null
          || request.resource.data.paymentMethod in ['cash', 'card', 'bankTransfer']
        )
        && (
          !request.resource.data.keys().hasAny([
            'photoStoragePath',
            'photoFileName',
            'photoContentType'
          ])
          || (
            request.resource.data.keys().hasAll([
              'photoStoragePath',
              'photoFileName',
              'photoContentType'
            ])
            && request.resource.data.photoStoragePath
              == 'users/' + userId + '/expenses/' + expenseId + '/receipt'
            && request.resource.data.photoFileName is string
            && request.resource.data.photoFileName.size() > 0
            && request.resource.data.photoFileName.size() <= 120
            && request.resource.data.photoContentType in [
              'image/jpeg',
              'image/png',
              'image/webp'
            ]
          )
        )
        && request.resource.data.occurredAt is timestamp
        && request.resource.data.createdAt is timestamp
        && request.resource.data.updatedAt is timestamp;
    }

    function validSpendingLimits() {
      return request.resource.data.keys().hasOnly([
          'dailyLimitCents',
          'monthlyLimitCents',
          'excludeIncome',
          'emailAlertsEnabled',
          'alertThresholds',
          'alertState',
          'baseCurrency',
          'updatedAt'
        ])
        && request.resource.data.keys().hasAll([
          'dailyLimitCents',
          'monthlyLimitCents',
          'excludeIncome',
          'emailAlertsEnabled',
          'alertThresholds',
          'baseCurrency',
          'updatedAt'
        ])
        && (
          request.resource.data.dailyLimitCents == null
          || (
            request.resource.data.dailyLimitCents is int
            && request.resource.data.dailyLimitCents > 0
            && request.resource.data.dailyLimitCents <= 100000000000
          )
        )
        && (
          request.resource.data.monthlyLimitCents == null
          || (
            request.resource.data.monthlyLimitCents is int
            && request.resource.data.monthlyLimitCents > 0
            && request.resource.data.monthlyLimitCents <= 100000000000
          )
        )
        && request.resource.data.excludeIncome is bool
        && request.resource.data.emailAlertsEnabled is bool
        && request.resource.data.alertThresholds is list
        && (
          !request.resource.data.keys().hasAny(['alertState'])
          || request.resource.data.alertState is map
        )
        && request.resource.data.baseCurrency in ['EUR', 'USD', 'GBP', 'CHF', 'JPY', 'CAD', 'AUD']
        && request.resource.data.updatedAt is timestamp;
    }

    function validCustomCategories() {
      return request.resource.data.keys().hasOnly([
          'categories',
          'updatedAt'
        ])
        && request.resource.data.keys().hasAll([
          'categories',
          'updatedAt'
        ])
        && request.resource.data.categories is list
        && request.resource.data.updatedAt is timestamp;
    }

    function validBalanceBaseline() {
      return request.resource.data.keys().hasOnly([
          'amountCents',
          'currency',
          'effectiveDate',
          'source',
          'institution',
          'updatedAt'
        ])
        && request.resource.data.keys().hasAll([
          'amountCents',
          'currency',
          'effectiveDate',
          'source',
          'institution',
          'updatedAt'
        ])
        && request.resource.data.amountCents is int
        && request.resource.data.amountCents >= -100000000000
        && request.resource.data.amountCents <= 100000000000
        && request.resource.data.currency in ['EUR', 'USD', 'GBP', 'CHF', 'JPY', 'CAD', 'AUD']
        && request.resource.data.effectiveDate is string
        && request.resource.data.effectiveDate.size() == 10
        && request.resource.data.source in ['manual', 'statement']
        && (
          request.resource.data.institution == null
          || request.resource.data.institution in ['eurobank', 'piraeus', 'unknown']
        )
        && request.resource.data.updatedAt is timestamp;
    }

    function validRecurringExpense() {
      return request.resource.data.keys().hasOnly([
          'description',
          'amountCents',
          'category',
          'transactionType',
          'paymentMethod',
          'currency',
          'frequency',
          'startDate',
          'nextOccurrenceAt',
          'active',
          'createdAt',
          'updatedAt'
        ])
        && request.resource.data.keys().hasAll([
          'description',
          'amountCents',
          'category',
          'transactionType',
          'paymentMethod',
          'currency',
          'frequency',
          'startDate',
          'nextOccurrenceAt',
          'active',
          'createdAt',
          'updatedAt'
        ])
        && request.resource.data.description is string
        && request.resource.data.description.size() > 0
        && request.resource.data.description.size() <= 120
        && request.resource.data.amountCents is int
        && request.resource.data.amountCents > 0
        && request.resource.data.amountCents <= 100000000000
        && request.resource.data.category is string
        && request.resource.data.category.size() > 0
        && request.resource.data.category.size() <= 50
        && request.resource.data.transactionType in ['expense', 'income']
        && request.resource.data.currency in ['EUR', 'USD', 'GBP', 'CHF', 'JPY', 'CAD', 'AUD']
        && (
          request.resource.data.paymentMethod == null
          || request.resource.data.paymentMethod in ['cash', 'card', 'bankTransfer']
        )
        && request.resource.data.frequency in ['weekly', 'monthly']
        && request.resource.data.startDate is timestamp
        && request.resource.data.nextOccurrenceAt is timestamp
        && request.resource.data.active is bool
        && request.resource.data.createdAt is timestamp
        && request.resource.data.updatedAt is timestamp;
    }

    match /users/{userId} {
      allow read, delete: if signedInAs(userId);
      allow create: if signedInAs(userId) && validProfile();
      allow update: if signedInAs(userId)
        && validProfile()
        && request.resource.data.createdAt == resource.data.createdAt;

      match /expenses/{expenseId} {
        allow read, delete: if signedInAs(userId);
        allow create: if signedInAs(userId) && validExpense(userId, expenseId);
        allow update: if signedInAs(userId)
          && validExpense(userId, expenseId)
          && request.resource.data.createdAt == resource.data.createdAt;
      }

      match /recurring-expenses/{scheduleId} {
        allow read, delete: if signedInAs(userId);
        allow create: if signedInAs(userId) && validRecurringExpense();
        allow update: if signedInAs(userId)
          && validRecurringExpense()
          && request.resource.data.createdAt == resource.data.createdAt;
      }

      match /settings/spending-limits {
        allow read: if signedInAs(userId);
        allow create, update: if signedInAs(userId) && validSpendingLimits();
        allow delete: if signedInAs(userId);
      }

      match /settings/custom-categories {
        allow read: if signedInAs(userId);
        allow create, update: if signedInAs(userId) && validCustomCategories();
        allow delete: if signedInAs(userId);
      }

      match /settings/balance-baseline {
        allow read: if signedInAs(userId);
        allow create, update: if signedInAs(userId) && validBalanceBaseline();
        allow delete: if signedInAs(userId);
      }

      match /settings/exchange-rates {
        allow read, write: if signedInAs(userId);
      }
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
